<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".screens.UserWorkoutsActivity">

    <!-- Header with Gradient Background -->
    <RelativeLayout
        android:id="@+id/header_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/fit_accent"
        android:padding="24dp">

        <TextView
            android:id="@+id/workout_header_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="16dp"
            android:text="@string/your_workout_name"
            android:textColor="@color/fit_primary"
            android:textSize="26sp"
            android:textStyle="bold"
            tools:text="האימון שלך, [שם משתמש]" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/workout_header_title"
            android:layout_alignEnd="@id/workout_header_title"
            android:layout_marginTop="4dp"
            android:text="@string/keep_it_up"
            android:textColor="@color/fit_primary"
            android:textSize="18sp" />

    </RelativeLayout>

    <!-- Layout for Completion Message -->
    <LinearLayout
        android:id="@+id/ll_completion_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/header_layout"
        android:layout_centerInParent="true"
        android:layout_marginTop="80dp"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        android:visibility="gone">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_check"
            app:tint="#2196F3"
            android:layout_marginBottom="16dp"/>

        <TextView
            android:id="@+id/tv_completion_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="סיימת את האימון להיום!\nכל הכבוד על ההתמדה 💪"
            android:textColor="#2196F3"
            android:textSize="22sp"
            android:textStyle="bold"
            android:lineSpacingExtra="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="נתראה מחר לאימון נוסף"
            android:textColor="#757575"
            android:textSize="16sp" />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/workouts_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/header_layout"
        android:background="@color/white"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        android:paddingBottom="8dp" />

</RelativeLayout>